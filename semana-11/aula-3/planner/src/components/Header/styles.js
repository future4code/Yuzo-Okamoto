import styled from 'styled-components';

const SHeader = styled.header`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32px 0;
  > div {
    padding: 0 16px;
  }
`;

export { SHeader };
