import styled from 'styled-components';

const Section = styled.section`
  flex: 1;
`;

const Title = styled.div`
  border-bottom: 1px solid black;
`;

const Body = styled.div`
  span {
    cursor: pointer;
  }
`;

export { Section, Title, Body };
